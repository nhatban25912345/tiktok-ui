import classNames from 'classnames/bind';
import styles from './VideoOnFocus.module.scss';

const cx = classNames.bind(styles);

function VideoCard() {
    return (
        <div className={cx("wrapper")}>
            
        </div>
    )
}

export default VideoCard;
